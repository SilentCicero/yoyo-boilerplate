import { createLogic } from 'redux-logic';

import { {{spaceListComma constants}} } from './constants';

export default createLogic({
  type: 'SOME_TYPE',
  latest: true,

  // process({ getState, action }, dispatch, done) {
  // },
});
