import { createSelector } from 'reselect';

export const select{{properCase name}} = state => state.{{camelCase name}};

export const select{{properCase name}}Property = createSelector(
  select{{properCase name}},
  substate => substate,
);
